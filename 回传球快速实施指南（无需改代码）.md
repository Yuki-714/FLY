# 回传球快速实施指南（无需修改代码）

## 🎯 目标

让开球机器人回传球给队友，而不是直接射门。

## ✅ 最简单方案：修改现有配置

### 方案选择

你有两个现成的开球策略可以修改：
1. **diamondKickOff5v5** - 菱形阵型（推荐）
2. **arrowKickOff5v5** - 箭头阵型

我推荐修改 **diamondKickOff5v5**，因为它的阵型更适合回传球。

---

## 📝 实施步骤

### 步骤1：备份原配置（可选）

```bash
cp Config/Behavior/KickOffs/diamondKickOff5v5.cfg \
   Config/Behavior/KickOffs/diamondKickOff5v5.cfg.backup
```

### 步骤2：修改 diamondKickOff5v5.cfg

打开文件：`Config/Behavior/KickOffs/diamondKickOff5v5.cfg`

**原内容**：
```cfg
{
  position = forward;
  pose = {
    rotation = 0deg;
    translation = {x = -centerCircleRadius - 250; y = 0;};
  };
  actions = [
    {
      type = wait;  // 原来是等待
    }
  ];
}
```

**修改为**：
```cfg
{
  position = forward;
  pose = {
    rotation = 180deg;  // 改为朝向己方（回传方向）
    translation = {x = -centerCircleRadius - 250; y = 0;};
  };
  actions = [
    {
      type = pass;  // 改为传球
      passTarget = [midfielderL, midfielderR];  // 传给左或右中场
    }
  ];
}
```

**同时修改中场的动作**：

**原内容**：
```cfg
{
  position = midfielderL;
  pose = {
    rotation = 0deg;
    translation = {x = xPosOwnPenaltyArea + 400; y = yPosLeftGoalArea;};
  };
  actions = [
    {
      type = mark;  // 原来是盯人
      markZone = {
        x = { max = -300; };
        y = { min = 300; };
      };
    }
  ];
}
```

**修改为**：
```cfg
{
  position = midfielderL;
  pose = {
    rotation = 0deg;
    translation = {x = xPosOwnPenaltyArea + 400; y = yPosLeftGoalArea;};
  };
  actions = [
    {
      type = shot;  // 改为射门
    }
  ];
}
```

**同样修改 midfielderR**：
```cfg
{
  position = midfielderR;
  pose = {
    rotation = 0deg;
    translation = {x = xPosOwnPenaltyArea + 400; y = yPosRightGoalArea;};
  };
  actions = [
    {
      type = shot;  // 改为射门
    }
  ];
}
```

### 步骤3：确认策略配置

打开 `Config/Behavior/Strategies/s5v5.cfg`

确认 `ownKickOffs` 部分包含 `diamondKickOff5v5`：

```cfg
ownKickOffs = [
  {
    type = diamondKickOff5v5;
    weight = 1;
  }
];
```

如果当前是 `directKickOff5v5`，改为：

```cfg
ownKickOffs = [
  {
    type = directKickOff5v5;
    weight = 0;  // 禁用直接射门
  },
  {
    type = diamondKickOff5v5;
    weight = 1;  // 启用回传球
  }
];
```

### 步骤4：测试

```bash
# 启动 SimRobot
cd Build/Linux/SimRobot/Develop
./SimRobot

# 加载场景
File → Open → Config/Scenes/Game.ros3

# 查看当前策略
get representation:StrategyStatus

# 应该显示：
# acceptedSetPlay: diamondKickOff5v5

# 开始测试
gc ready
gc set
gc playing
```

---

## 📋 完整的修改后配置

### diamondKickOff5v5.cfg（完整版）

```cfg
tactic = t121;

positions = [
  {
    position = forward;
    pose = {
      rotation = 180deg;  // 朝向己方（回传）
      translation = {x = -centerCircleRadius - 250; y = 0;};
    };
    actions = [
      {
        type = pass;
        passTarget = [midfielderL, midfielderR];
      }
    ];
  },
  {
    position = midfielderL;
    pose = {
      rotation = 0deg;
      translation = {x = xPosOwnPenaltyArea + 400; y = yPosLeftGoalArea;};
    };
    actions = [
      {
        type = shot;
      }
    ];
  },
  {
    position = midfielderR;
    pose = {
      rotation = 0deg;
      translation = {x = xPosOwnPenaltyArea + 400; y = yPosRightGoalArea;};
    };
    actions = [
      {
        type = shot;
      }
    ];
  }
];

priorityGroups = [
  {
    positions = [forward];
    priorities = [0];
  },
  {
    positions = [midfielderL, midfielderR];
    priorities = [1, 1];
  },
  {
    positions = [defender];
    priorities = [2];
  }
];

startPosition = forward;
lowestRequiredPriority = 0;
```

---

## 🔍 预期行为

### 时间轴

```
0s  → forward 走向球（位置：球后方约1000mm）
1s  → forward 到达球附近
2s  → forward 对齐传球姿态（朝向己方，rotation=180deg）
3s  → forward 执行传球动作
4s  → 球滚向 midfielderL 或 midfielderR
5s  → 中场接球
6s  → 中场对齐射门姿态
7s  → 中场射门
```

### 阵型图

```
己方球门 ←─────────────────────────────────────→ 对手球门
         -4500                0                4500

                                              ⚽ 对手球门
                                              ↑
         forward [PASS]                       |
         (-1000, 0) ●                         |
              ⚽ (0, 0)                        |
              |                               |
              | 回传                          |
              ↓                               |
    midfielderL [SHOT]    midfielderR [SHOT] |
    (-3100, 700) ●        (-3100, -700) ●    |
         ↓                      ↓             |
         射门                   射门           |
                                              |
         defender                             |
         (默认位置) ●                          |
                                              |
己方球门 ←─────────────────────────────────────
```

---

## ⚠️ 常见问题

### 问题1：forward 还是射门而不是传球

**可能原因**：
- 配置文件没有保存
- 配置文件语法错误

**解决方法**：
1. 检查文件是否保存
2. 检查语法（特别是括号、分号）
3. 重启 SimRobot

### 问题2：传球失败，球没有到达中场

**可能原因**：
- 中场位置没有机器人
- 传球距离太远

**解决方法**：
1. 检查机器人数量（至少需要2个）
2. 调整中场位置，让它更靠近球：
   ```cfg
   translation = {x = xPosOwnPenaltyArea + 1000; y = yPosLeftGoalArea;};
   ```

### 问题3：中场接球后不射门

**可能原因**：
- 中场的 action 没有设置为 shot

**解决方法**：
1. 确认中场的 actions 包含：
   ```cfg
   actions = [
     {
       type = shot;
     }
   ];
   ```

### 问题4：机器人不动

**可能原因**：
- 配置文件语法错误
- 策略没有正确加载

**解决方法**：
1. 检查配置文件语法
2. 查看 SimRobot 控制台的错误信息
3. 使用 `get representation:StrategyStatus` 检查当前策略

---

## 🎨 调试技巧

### 启用调试绘制

```
# 查看站位
vid field module:StrategyBehaviorControl:setPlay

# 查看传球评估
vid upper module:PassEvaluationProvider:heatmap

# 查看机器人状态
get representation:BehaviorStatus
get representation:SkillRequest
```

### 查看日志

在 SimRobot 控制台中查看输出：
- 传球目标选择
- 技能执行状态
- 错误信息

---

## 🔄 恢复原配置

如果想恢复原来的配置：

```bash
# 如果你做了备份
cp Config/Behavior/KickOffs/diamondKickOff5v5.cfg.backup \
   Config/Behavior/KickOffs/diamondKickOff5v5.cfg

# 或者从 git 恢复
git checkout Config/Behavior/KickOffs/diamondKickOff5v5.cfg
```

---

## 📊 对比：修改前 vs 修改后

### 修改前（原 diamondKickOff5v5）

| 位置 | 动作 | 说明 |
|------|------|------|
| forward | wait | 等待比赛开始 |
| midfielderL | mark | 盯人防守 |
| midfielderR | mark | 盯人防守 |

**特点**：防守型，不主动进攻

### 修改后（回传球版本）

| 位置 | 动作 | 说明 |
|------|------|------|
| forward | pass → [midfielderL, midfielderR] | 回传给中场 |
| midfielderL | shot | 接球后射门 |
| midfielderR | shot | 接球后射门 |

**特点**：进攻型，回传配合

---

## ✨ 优化建议

### 1. 调整中场位置（如果传球距离太远）

```cfg
translation = {x = -1500; y = yPosLeftGoalArea;};  // 更靠前
```

### 2. 添加第三个传球目标（增加成功率）

```cfg
passTarget = [midfielderL, midfielderR, defender];
```

### 3. 调整前锋朝向（如果传球角度不好）

```cfg
rotation = 160deg;  // 稍微偏一点
```

---

## 🎯 总结

### 需要修改的文件

1. ✅ `Config/Behavior/KickOffs/diamondKickOff5v5.cfg`
2. ✅ `Config/Behavior/Strategies/s5v5.cfg`（确认使用 diamondKickOff5v5）

### 不需要修改

- ❌ 源代码（.cpp, .h 文件）
- ❌ 不需要重新编译
- ❌ 不需要修改枚举定义

### 立即生效

- ✅ 修改配置文件后，重启 SimRobot 即可
- ✅ 或者在 SimRobot 中重新加载场景

---

**祝你成功实现回传球策略！⚽**
