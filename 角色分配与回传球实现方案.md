# 角色分配与回传球实现方案（纯配置方案）

## 一、角色分配机制分析

### 1. 系统架构

```
Strategy (s5v5.cfg)
    ↓
Tactic (t112, t121, t211)
    ↓
Position (forwardL, midfielder, defender等)
    ↓
Role (PlayBall, 其他角色)
    ↓
Skill (shoot, pass, dribble等)
```

### 2. 关键概念

#### Position（位置）
- 战术层面的位置：forwardL, forwardR, midfielder, defender等
- 在SetPlay中定义具体站位和动作
- 通过优先级组（priorityGroups）分配给机器人

#### Role（角色）
- 行为层面的角色：PlayBall（控球）, 其他支援角色
- **PlayBall角色**负责决策：射门、传球还是带球
- 只有**拥有球权的机器人**才会执行PlayBall角色

#### 从你的截图分析

```
position: goalkeeper
role: none
acceptedSetPlay: directKickOff5v5
```

这说明：
- 这个机器人被分配到了 `goalkeeper` 位置
- 它的角色是 `none`（不是PlayBall）
- 当前使用的开球策略是 `directKickOff5v5`

**关键发现**：只有被分配到 `startPosition`（开球位置）的机器人才会获得球权，执行PlayBall角色！

---

## 二、为什么你的回传球配置可能不工作

### 问题分析

你创建的配置文件：
```cfg
{
  position = forwardL;
  actions = [
    {
      type = pass;
      passTarget = [midfielder];
    }
  ];
}
```

**问题**：`pass` 动作是 **SetPlay Action**，它会直接调用 `PassAction::execute()`，**绕过了 PlayBall 角色的决策逻辑**！

### SetPlay Action vs PlayBall Role

#### SetPlay Action（你的配置）
```cpp
// SetPlayActions.cpp
SkillRequest PassAction::execute(...)
{
  // 直接传球给指定目标
  return SkillRequest::Builder::passTo(lastSelectedPassTarget);
}
```

- ✅ 优点：强制传球，不会射门
- ❌ 缺点：传球目标必须存在，否则动作失败
- ❌ 缺点：不会评估传球质量

#### PlayBall Role（默认行为）
```cpp
// PlayBall.cpp
SkillRequest PlayBall::smashOrPass(...)
{
  // 评估射门、传球、带球的得分
  // 选择最优动作
  if(bestActionRating > shootThreshold)
    return shoot();
  else if(passTarget)
    return passTo(passTarget);
  else
    return dribble();
}
```

- ✅ 优点：智能决策，选择最优动作
- ✅ 优点：自动寻找最佳传球目标
- ❌ 缺点：可能选择射门而不是传球

---

## 三、解决方案：通过参数调整实现回传球

### 方案1：降低射门阈值，提高传球优先级（推荐）

**原理**：调整 PlayBall 的参数，让它更倾向于传球而不是射门。

#### 步骤1：创建 PlayBall 配置文件

创建文件：`Config/Scenarios/Default/playBall.cfg`

```cfg
// PlayBall 角色参数配置
// 目标：开球时优先传球而不是射门

alwaysShoot = false;  // 不要总是射门

// 射门阈值：提高到0.95，让射门变得更难
// 原值：0.8，现在需要更高的得分才会射门
shootThreshold = 0.95;

// 传球距离：调整为适合回传的距离
minPassDistance = 500;  // 降低最小传球距离（原值1500）
maxPassDistance = 3000;  // 降低最大传球距离，鼓励短传（原值4500）

// 决策惩罚：降低切换决策的惩罚，让系统更灵活
minPenalty = 0.05;  // 原值0.1
maxPenalty = 0.3;   // 原值0.8

// 搜索深度：增加传球链长度
maxSearchDepth = 2;  // 允许两次传球（原值1）

// 折扣因子：提高后续传球的价值
discountFactor = 0.95;  // 原值0.9

// 最小评分：降低最小评分要求
minRating = 0.001;  // 原值0.01
```

#### 步骤2：在场景配置中加载

编辑 `Config/Scenarios/Default/strategyBehaviorControl.cfg`（如果不存在则创建）：

```cfg
strategy = s5v5;

// 加载 PlayBall 参数
// 注意：这个语法可能需要根据实际系统调整
```

**或者**，如果系统支持，在机器人配置中加载：
`Config/Robots/fly16/playBall.cfg`

---

### 方案2：使用 SetPlay Action + 保护机制

**原理**：使用 `pass` 动作，但添加备选方案。

#### 修改开球配置

编辑 `Config/Behavior/KickOffs/backPassKickOff5v5.cfg`：

```cfg
tactic = t112;

positions = [
  {
    position = forwardL;
    pose = {
      rotation = 180deg;
      translation = {x = -400; y = 0;};
    };
    actions = [
      {
        type = pass;
        passTarget = [midfielder, forwardR];  // 多个目标，增加成功率
      }
    ];
  },
  {
    position = forwardR;
    pose = {
      rotation = 135deg;
      translation = {x = -800; y = -1000;};  // 更靠近，容易接球
    };
    actions = [
      {
        type = shot;  // 接球后射门
      }
    ];
  },
  {
    position = midfielder;
    pose = {
      rotation = 0deg;
      translation = {x = -1200; y = 0;};  // 更靠近，容易接球
    };
    actions = [
      {
        type = shot;  // 接球后射门
      }
    ];
  }
];

priorityGroups = [
  {
    positions = [forwardL];
    priorities = [0];
  },
  {
    positions = [midfielder, forwardR];  // 两者优先级相同，确保至少有一个
    priorities = [1, 1];
  },
  {
    positions = [defender];
    priorities = [2];
  }
];

startPosition = forwardL;
lowestRequiredPriority = 1;  // 要求至少有一个接球者
```

**关键改进**：
1. 多个传球目标：`[midfielder, forwardR]`
2. 接球者位置更近：更容易接到球
3. 优先级设置：确保至少有一个接球者

---

### 方案3：混合方案（最推荐）

**原理**：结合 SetPlay Action 和 PlayBall 参数调整。

#### 步骤1：创建特殊的开球配置

`Config/Behavior/KickOffs/smartBackPassKickOff5v5.cfg`：

```cfg
tactic = t112;

positions = [
  {
    position = forwardL;
    pose = {
      rotation = 180deg;  // 朝向己方
      translation = {x = -400; y = 0;};
    };
    // 不指定 actions，让 PlayBall 角色自动决策
    // 但通过参数调整，它会倾向于传球
  },
  {
    position = forwardR;
    pose = {
      rotation = 90deg;
      translation = {x = -800; y = -1200;};
    };
  },
  {
    position = midfielder;
    pose = {
      rotation = 0deg;
      translation = {x = -1500; y = 0;};  // 后方接球位置
    };
  }
];

priorityGroups = [
  {
    positions = [forwardL];
    priorities = [0];
  },
  {
    positions = [midfielder];
    priorities = [1];
  },
  {
    positions = [forwardR];
    priorities = [2];
  },
  {
    positions = [defender];
    priorities = [3];
  }
];

startPosition = forwardL;
lowestRequiredPriority = 0;
```

#### 步骤2：创建 PlayBall 参数文件

`Config/Scenarios/Default/playBall.cfg`：

```cfg
// 回传球优化参数

alwaysShoot = false;

// 关键：大幅提高射门阈值
shootThreshold = 0.99;  // 几乎不会射门

// 传球距离：适合回传
minPassDistance = 300;
maxPassDistance = 2500;

// 鼓励传球
minPenalty = 0.02;
maxPenalty = 0.2;
maxSearchDepth = 2;
discountFactor = 0.98;
```

---

## 四、实施步骤（纯配置方案）

### 步骤1：注册新的开球策略

编辑 `Src/Tools/BehaviorControl/Strategy/KickOff.h`：

```cpp
STREAMABLE_WITH_BASE(OwnKickOff, KickOff,
{
  ENUM(Type,
  {,
    directKickOff,
    directKickOff5v5,
    backPassKickOff5v5,        // 新增
    smartBackPassKickOff5v5,   // 新增
  });
  // ...
});
```

### 步骤2：更新策略配置

编辑 `Config/Behavior/Strategies/s5v5.cfg`：

```cfg
// ... 其他配置 ...

ownKickOffs = [
  {
    type = smartBackPassKickOff5v5;  // 使用新策略
    weight = 1;
  }
];

// 或者保留多个选项
ownKickOffs = [
  {
    type = directKickOff5v5;
    weight = 0;  // 禁用
  },
  {
    type = smartBackPassKickOff5v5;
    weight = 1;  // 启用
  }
];
```

### 步骤3：重新编译

```bash
cd Make/Linux
./compile
```

### 步骤4：测试

在 SimRobot 中：

```
# 查看当前策略
get representation:StrategyStatus

# 应该显示：
# acceptedSetPlay: smartBackPassKickOff5v5
```

---

## 五、不需要修改源代码的替代方案

### 方案A：使用现有的 diamondKickOff5v5 并修改

`diamondKickOff5v5` 已经存在，你可以直接修改它：

编辑 `Config/Behavior/KickOffs/diamondKickOff5v5.cfg`：

```cfg
tactic = t121;

positions = [
  {
    position = forward;
    pose = {
      rotation = 180deg;  // 改为朝向己方
      translation = {x = -centerCircleRadius - 250; y = 0;};
    };
    actions = [
      {
        type = pass;  // 改为传球
        passTarget = [midfielderL, midfielderR];
      }
    ];
  },
  {
    position = midfielderL;
    pose = {
      rotation = 0deg;
      translation = {x = xPosOwnPenaltyArea + 400; y = yPosLeftGoalArea;};
    };
    actions = [
      {
        type = shot;  // 改为射门
      }
    ];
  },
  {
    position = midfielderR;
    pose = {
      rotation = 0deg;
      translation = {x = xPosOwnPenaltyArea + 400; y = yPosRightGoalArea;};
    };
    actions = [
      {
        type = shot;  // 改为射门
      }
    ];
  }
];

// ... 其他配置保持不变 ...
```

然后在 `s5v5.cfg` 中使用：

```cfg
ownKickOffs = [
  {
    type = diamondKickOff5v5;  // 使用修改后的配置
    weight = 1;
  }
];
```

**优点**：
- ✅ 不需要修改源代码
- ✅ 不需要重新编译
- ✅ 立即生效

**缺点**：
- ⚠️ 覆盖了原有的 diamondKickOff5v5 配置

---

### 方案B：使用 arrowKickOff5v5 并修改

编辑 `Config/Behavior/KickOffs/arrowKickOff5v5.cfg`：

```cfg
tactic = t211;

positions = [
  {
    position = forward;
    pose = {
      rotation = 180deg;  // 朝向己方
      translation = {x = -centerCircleRadius - 250; y = 0;};
    };
    actions = [
      {
        type = pass;  // 传球
        passTarget = [midfielder];
      }
    ];
  },
  {
    position = midfielder;
    pose = {
      rotation = 0deg;
      translation = {x = xPosOwnPenaltyArea + 750; y = 300;};
    };
    actions = [
      {
        type = shot;  // 射门
      }
    ];
  },
  // ... 其他位置 ...
];

// ... 其他配置 ...
```

然后在 `s5v5.cfg` 中：

```cfg
ownKickOffs = [
  {
    type = arrowKickOff5v5;
    weight = 1;
  }
];
```

---

## 六、推荐方案总结

### 🥇 最推荐：方案A（修改 diamondKickOff5v5）

**原因**：
1. ✅ 不需要修改源代码
2. ✅ 不需要重新编译
3. ✅ 立即生效
4. ✅ 使用现有的枚举值

**步骤**：
1. 修改 `Config/Behavior/KickOffs/diamondKickOff5v5.cfg`
2. 修改 `Config/Behavior/Strategies/s5v5.cfg`
3. 重启 SimRobot 测试

### 🥈 次推荐：方案B（修改 arrowKickOff5v5）

**原因**：
1. ✅ 不需要修改源代码
2. ✅ 不需要重新编译
3. ✅ 立即生效
4. ⚠️ 但 arrowKickOff5v5 可能在其他地方被使用

### 🥉 备选：创建新配置 + 修改源代码

**原因**：
1. ✅ 不影响现有配置
2. ✅ 更清晰的命名
3. ❌ 需要修改源代码
4. ❌ 需要重新编译

---

## 七、测试检查清单

### 1. 配置文件检查

- [ ] `diamondKickOff5v5.cfg` 已修改
- [ ] `s5v5.cfg` 中 `ownKickOffs` 已更新
- [ ] 传球目标位置有机器人占据

### 2. SimRobot 测试

```bash
# 启动 SimRobot
cd Build/Linux/SimRobot/Develop
./SimRobot

# 加载场景
File → Open → Config/Scenes/Game.ros3

# 查看策略状态
get representation:StrategyStatus

# 应该显示：
# acceptedSetPlay: diamondKickOff5v5

# 启用调试绘制
vid field module:StrategyBehaviorControl:setPlay
vid upper module:PassEvaluationProvider:heatmap

# 开始比赛
gc ready
gc set
gc playing
```

### 3. 预期行为

1. ✅ forward 走向球
2. ✅ forward 朝向己方（rotation = 180deg）
3. ✅ forward 传球给 midfielder
4. ✅ midfielder 接球
5. ✅ midfielder 射门

### 4. 常见问题

**问题1**：forward 还是射门而不是传球
- **原因**：`pass` 动作可能没有生效
- **解决**：检查 actions 配置是否正确

**问题2**：传球失败
- **原因**：传球目标位置没有机器人
- **解决**：检查 priorityGroups，确保 midfielder 优先级足够高

**问题3**：机器人不动
- **原因**：配置文件语法错误
- **解决**：检查 .cfg 文件语法，特别是括号和分号

---

## 八、完整配置示例

### diamondKickOff5v5.cfg（修改后）

```cfg
tactic = t121;

positions = [
  {
    position = forward;
    pose = {
      rotation = 180deg;  // 朝向己方（回传）
      translation = {x = -centerCircleRadius - 250; y = 0;};
    };
    actions = [
      {
        type = pass;
        passTarget = [midfielderL, midfielderR];  // 传给左或右中场
      }
    ];
  },
  {
    position = midfielderL;
    pose = {
      rotation = 0deg;
      translation = {x = xPosOwnPenaltyArea + 400; y = yPosLeftGoalArea;};
    };
    actions = [
      {
        type = shot;  // 接球后射门
      }
    ];
  },
  {
    position = midfielderR;
    pose = {
      rotation = 0deg;
      translation = {x = xPosOwnPenaltyArea + 400; y = yPosRightGoalArea;};
    };
    actions = [
      {
        type = shot;  // 接球后射门
      }
    ];
  }
];

priorityGroups = [
  {
    positions = [forward];
    priorities = [0];
  },
  {
    positions = [midfielderL, midfielderR];
    priorities = [1, 1];
  },
  {
    positions = [defender];
    priorities = [2];
  }
];

startPosition = forward;
lowestRequiredPriority = 0;
```

### s5v5.cfg（修改后）

```cfg
// ... 其他配置 ...

ownKickOffs = [
  {
    type = diamondKickOff5v5;  // 使用修改后的回传球策略
    weight = 1;
  }
];

// ... 其他配置 ...
```

---

**总结**：最简单的方案是直接修改现有的 `diamondKickOff5v5.cfg` 或 `arrowKickOff5v5.cfg`，不需要修改任何源代码，立即生效！
